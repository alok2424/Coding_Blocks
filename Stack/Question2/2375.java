class Solution{
    public static String Smallest_Num_From_DI(String s){
        int[] arr = new int[s.length()+1];
        int c= 1;
        Stack<Integer> st = new Stack<>();
        for(int i = 0; i<= s.length();i++){
            if(i == s.length() || s.charAt(i)=='I'){
                arr[i] = c++;
                while(!st.)
            }
        }
    }
}